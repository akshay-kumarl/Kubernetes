apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: acluster
  region: ap-south-1

availabilityZones:
  - ap-south-1a
  - ap-south-1b

managedNodeGroups:
  - name: worker-nodes
    instanceType: t2.medium
    desiredCapacity: 2
    minSize: 1
    maxSize: 3
